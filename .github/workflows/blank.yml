name: Scheduled API Data Fetch and Commit

on:
  schedule:
    - cron: "*/5 * * * *"  # Runs every 5 minutes
jobs:
  run-every-5-minutes:
    runs-on: ubuntu-latest

    steps:
    - name: Run a script
      run: |
        echo "This is running every 5 minutes at $(date)"
